---
description: diff-tab Commit & Push 完成后自动合并到 test 并推送，再切回原分支（main/staging 除外）
alwaysApply: true
---

# Commit & Push 后自动合并到 test

当用户执行 **Execute the selected diff-tab commit-and-push action**（diff 面板 Commit & Push）后，助手在**本次 commit 与 push 已成功完成**的前提下，**立即**继续执行以下流程（不要只口头说明）：

## 跳过条件

当前分支为以下之一时**不执行**本流程：

- `main`
- `staging`

## 执行步骤

1. 记录当前分支名：`SOURCE_BRANCH=$(git branch --show-current)`
2. `git fetch origin`
3. `git checkout test`
4. 若本地 `test` 落后于 `origin/test`，先 `git pull --ff-only origin test`（fast-forward 失败再按情况处理，不要强推）
5. `git merge "$SOURCE_BRANCH" --no-edit`
   - 若有冲突：解决冲突 → `git add` → `git commit --no-edit`（或合适的 merge commit message），再继续
6. `git push origin test`
7. `git checkout "$SOURCE_BRANCH"`

## 要求

- 全程用 `Shell` 实际执行，权限按需包含 `git_write`、`full_network`（或 `all`）。
- 不要修改用户未要求的无关文件；合并冲突只处理冲突文件。
- 流程结束向用户简要汇报：源分支、merge commit、test 推送结果、已切回的分支。
- 若 commit/push 本身失败，**不要**执行本流程。
